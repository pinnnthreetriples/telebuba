dist
coverage
.cov-tmp
node_modules
package-lock.json
openapi.json

# Канон дизайн-системы: половину его разметки собирает scripts/design-system-doc.mjs,
# и эта половина обязана быть побайтово предсказуемой, иначе `ds:doc:check` начнёт
# спорить с prettier вместо того, чтобы ловить расхождение с токенами. Вторая
# половина написана руками, и переформатировать её значит потерять её вёрстку.
docs/design-system.html

# GitHub Work
Repository: `pinnnthreetriples/telebuba`; Project: owner `pinnnthreetriples`, number `2`.

Status flow: `Backlog → Ready → In progress → In review → Done`. Refresh the board first, keep one item in progress, move before work, open a PR with `Closes #N`, then mark Done after merge. Promote Backlog only when scope, completion criteria and dependencies are clear.

```bash
gh project item-list 2 --owner pinnnthreetriples --format json
gh issue view <N> --comments
gh issue create --repo pinnnthreetriples/telebuba --title "..." --body "..." --project telebuba
gh issue comment <N> --repo pinnnthreetriples/telebuba --body "..."
```

Board columns—not triage labels—define readiness. Never use machine-specific executable paths.

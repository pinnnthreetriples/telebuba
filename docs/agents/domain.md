# Domain Memory
Read `.mex/ROUTER.md`, then only its task route. Current knowledge lives in four files: `architecture.md`, `conventions.md`, `frontend.md`, and `runtime.md`; setup/commands live in `setup.md`; repeatable work lives in `patterns/`.

Trust code/tests over memory. Current state stays in `ROUTER.md`; rationale/history comes from `mex timeline`, git, and merged PRs.

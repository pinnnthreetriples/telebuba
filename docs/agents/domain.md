# Domain Memory

Read .mex/ROUTER.md, then only its matching task route and at most one relevant pattern. Do not create a parallel CONTEXT.md/ADR memory tree.

Trust code/tests over memory. Current state belongs in the router only when cross-task routing needs it; durable facts live in the matching context; rationale/history comes from a small mex timeline query, Git, issues and merged PRs.

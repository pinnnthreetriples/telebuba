# Pattern Index

Lookup table for all pattern files in this directory. Check here before starting any task — if a pattern exists, follow it.

| Pattern | Use when |
|---------|----------|
| [add-feature.md](add-feature.md) | Adding or extending a user-facing feature (UI in `features/` + business logic in `services/`) |
| [add-service.md](add-service.md) | Adding or extending a business-logic module/package in `services/` |
| [add-telegram-task.md](add-telegram-task.md) | Writing code that performs a Telegram action through the typed-action executor |
| [add-warming-job.md](add-warming-job.md) | Adding or changing warming runtime work in the per-account asyncio-task model |

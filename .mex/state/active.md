---
name: active-state
description: Live project state — what works, what is not yet built, known issues. Updated by the agent in the Record step of GROW after meaningful work.
last_updated: 2026-06-28
---

# Active State

This file is the only place that should change after every meaningful task. `ROUTER.md` stays stable.

## Working

- `pyproject.toml` + `uv.lock` resolved; local target is Python 3.13.x.
- Runtime stack is installed and import-verified: NiceGUI, SQLAlchemy/SQLite, Telethon, python-socks, httpx, python-dotenv, pydantic, pydantic-settings, loguru, sentry-sdk, anyio. APScheduler and structlog are intentionally unused/removed.
- Dev toolchain is wired: ruff, ty, pytest+asyncio+cov, hypothesis, bandit, pip-audit, semgrep, deptry, vulture, radon, pre-commit, respx, factory-boy, aislop.
- Strict quality gates are active: warnings-as-errors, pytest strict config, branch coverage floor 90%, ruff `ALL`, ty strict unresolved-reference rules, bandit, deptry, vulture, radon, semgrep, pip-audit, and zero-tolerance aislop. Suite currently at 575 tests green, coverage ≥ 90%.
- `.pre-commit-config.yaml` and CI run the same major gates. Aislop runs at pre-push and in CI with Node available.
- `.gitignore` covers `.env`, `*.session`, `*.db`, `*.log`, and tool caches.
- `main.py` is the NiceGUI composition root. It registers accounts/warming/logs pages, runs on `UI__PORT`, calls `services.warming.reconcile_warming_runtime()` on startup, and shuts runtime tasks down gracefully.
- `core/config.py` uses nested `pydantic-settings` namespaces: `TELEGRAM__*`, `UI__*`, `DB__*`, `PROXY__*`, `PROFILE_MEDIA__*`, `LOGGING__*`, `WARMING__*`, `GEMINI__*`, `TRUST__*`.
- `.env.example` has full config coverage and is checked by `tests/test_architecture.py`.
- `core/db.py` is now shared SQLite plumbing only: metadata, table definitions, engine lifecycle, generic row helpers, and compatibility re-exports. Schema evolution lives in `core/migrations.py` as a versioned, append-only registry stamped in a `schema_version` table — `apply_migrations()` runs every unstamped migration idempotently, so legacy databases that already carry the columns are stamped without errors.
- Per-aggregate DB queries live in `core/repositories/`: accounts, warming, logs, content, device_fingerprint, dialogues, spam_status, and the `neurocomment/` package (campaigns, channel↔campaign links with a DB-level "one active campaign per channel" invariant, account↔campaign m2m, linked-group cache, readiness, comment claims).
- SQLite foreign keys are enabled on every new connection via `PRAGMA foreign_keys=ON`.
- `core/telegram_client/` is a gateway package. Public API is re-exported from `core.telegram_client`; implementation is split into focused private modules.
- `core/gemini.py` is the HTTP gateway for Gemini and returns typed results.
- `core/logging.py` is live: loguru diagnostic file + SQLite `logs` table + optional Sentry. `log_event()` is best-effort and does not break business operations when a sink fails.
- `core/tdata_import.py` converts uploaded `tdata.zip` to Telethon `.session` files with safe-extract guardrails.
- `services/accounts/` is a thin re-export package. Implementations live in per-concern submodules: `lifecycle.py` (registration + geo), `sessions.py` (`.session` and tdata import + liveness check, with the `_tdata`/`_uploads`/`_table` helpers), `proxy.py`, `profile.py`, `media.py`. Tests monkeypatch external collaborators on the owning submodule (`services.accounts.sessions.convert_tdata_zip`, `services.accounts.proxy.check_proxy_connectivity`, `services.accounts.profile.execute`, `services.accounts.media.execute`).
- `import_account_tdata` returns a `TdataImportResult` Pydantic wrapper rather than a raw `list[AccountRead]`, keeping the service boundary Pydantic-only and leaving room for per-import metadata. Same treatment applied to `services.dialogues.get_partners` → `DialoguePartnersResult` and `services.dialogues.assign_pairs` → `DialoguePairsResult`.
- `tests/test_architecture.py` walks each layer with `rglob("*.py")` (no auto-skip for `__init__.py`), adds a cross-feature isolation test, and a regression test that the package submodules (`services/accounts/`, `services/warming/`, `features/accounts/`, `features/warming/`, `core/telegram_client/`, `core/repositories/`) are actually reached by the scan.
- `features/accounts/` is a NiceGUI page package, split into focused rendering modules: `_header.py` (toolbar + nav), `_metrics.py` (metric tiles), `_table_section.py` (search + table), `_controller.py` (event handlers / page state), `_page.py` (route + wiring), `_dialogs.py`, `_table.py`. `__init__.py` is re-export only — `register_accounts_page` is the public entry. The controller catches `ValueError` from `check_account_session` and surfaces it via `ui.notify` instead of letting it escape into NiceGUI's logs.
- `services/warming/` is a package. Submodules: `channels.py`, `settings_store.py`, `board.py`, `pacing.py`, `_seams.py`, `_state.py`, `_chat.py`, `_cycle.py`, `_transitions.py`, `_loop.py`, `_runner.py`, `_purge.py`, `_runtime.py`.
- `features/warming/` is a UI-only package for config cards, channel UI, board rendering, and activity/log UI.
- Warming runtime model is resolved: per-account `asyncio.Task`s owned by `services/warming/_runtime.py`, not APScheduler.
- `telegram_outbox` is resolved/dropped. Current model is direct typed executor + persisted per-cycle runtime state. Reopen queue/outbox only for multi-process execution.
- Board N+1 was removed. `load_board()` bulk-loads required signals once and keeps card enrichment pure.
- Scaffold memory refresh completed on 2026-06-16: `.mex/AGENTS.md`, architecture/conventions/decisions/stack/services/logging/telegram/warming contexts, and related patterns now describe package-based domains, direct executor, repositories, no structlog, and no APScheduler for warming.
- Warming audit (#98–#102 + 6 review bugs): fixed scheduling/ban-risk, board indicators, cycle resilience, localization, channel feedback, reconcile quarantine recovery, cap=1 lockout, trust-ceiling progress, one-sided faded-pair DMs, case-sensitive invite dedup. 536 tests green.
- Accounts QA audit PR #104: 15 findings fixed — single-source RU localization, username=None on unchanged, error snapshot not cached, footer hides on success only, story kind from extension, delete errors translated, search debounce, uploader CSS scoped. 543 tests green.
- Animated Warming Pipeline UI (branch `feature/warming-pipeline-ui`) — **Done**: per-account 6-step horizontal cycle pipeline (online → join → read → react → chat → sleep) with a live active-step detail panel and a cycle summary bar, rendered inside every warming-column kanban card. Reads only the existing polled `WarmingAccountState` (no new schema, no new polling, no layer boundary crossings). Implementation lives in the new `features/warming/_pipeline.py` (UI-only); styles + relative-ETA helper in `features/warming/_board_styling.py`; CSS keyframes (`tb-flow`, `tb-step-spin`) in `features/warming/__init__.py` via `ui.add_css(shared=True)`. The `_render_card()` call site is gated by `if card.state != "idle"` so idle-column cards remain pixel-for-pixel unchanged. `_relative_eta()` was relocated from `_board.py` to `_board_styling.py` so existing callers resolve unchanged. File Map updated in `.mex/AGENTS.md`. `ruff check` clean (0 new findings vs. main). `ty check` clean (0 new findings vs. main — single new `unresolved-import: nicegui has no member 'ui'` suppressed with `# ty: ignore[unresolved-import]`, matching the pre-existing env issue shared by 5+ sibling warming files).
- Warming card design polish + review cleanup (PR #106, same branch) — **Done**: card reworked to match the reference (coloured left status stripe, prominent trust score, Title-case health chips, status line above the detail box with a live animated ticker, calm-blue resting «Сон» node, 3-row sleep detail with real wake-up ETA, progress-bar min-width). Review follow-up: removed 7 orphaned styling constants and the unused `_check_spam`; per-state text/glyph moved from if/elif chains to dict lookups; the pure pipeline resolvers (`_active_step` / `_step_kind` / `_connector_kind` / `_next_active_index`) un-`pragma`'d and unit-tested in `tests/features/test_warming_pipeline.py`. 598 tests green; aislop 0 warnings; CI green.
- Active rail mid-cycle progress (issue #107) — **Done**: the engine now persists live progress *during* a healthy cycle (previously `last_action` was failure-only, so the active rail was stuck). `services/warming/_loop.py` seeds `last_action="set_online"` on the `cycle_started` write and passes a monotonic, CAS-guarded `on_step` closure (forward-only via `_PROGRESS_STEPS`) into `run_one_cycle`; `services/warming/_cycle.py` fires `on_step` after each successful step (set_online/join/read/react/send_dm). No schema/migration — `last_action` is reused, disambiguated by `state` (active = current step, error = failed step). Resolver `features/warming/_pipeline.py`: `_ACTION_TO_STEP` gains `read`/`react`, `_next_active_index` maps the running step directly (no `+1`; `None`→online). Status-line labels for `read`/`react` added. The board's 4s poll + `last_action` in `_card_signature` surface it. Review follow-up (PR #109): the `on_step` write is now wrapped in `try/except` + log so a transient SQLite-lock raise can never abort a live cycle / park a healthy account in `error` (the write is cosmetic, best-effort); `cycle_started` now seeds `last_channel=None` so the active detail panel can't surface a prior cycle's stale channel. Forward-only rail stick on multi-channel cycles left as the intended trade-off. 609 tests green; ruff/ty/aislop clean.
- Per-account activity log on cards (branch `feat/warming-card-activity-log`) — **Done**: each warming kanban card gains an expandable «Логи аккаунта» toggle that opens a dark "terminal" panel showing **all** of that account's recent events newest-first (every step the account does). No backend/logging change — `core/telegram_client/_actions.py` already logs `telegram_{action_type}` at INFO per step, scoped by `account_id`; the panel queries `services.logs.load_logs_page(LogFilter(account_id=…, limit=settings.warming.card_log_limit))`. New UI-thin module `features/warming/_termlog.py` (CSS via `ui.add_css(shared=True)`, event→RU-label/icon map, pure `_event_label`/`_format_extra`/`_toggle_expanded`). Expansion state + fetched rows + a log-signature live on `_BoardContext` (not the per-card refreshable) so an open panel survives the 4s poll; the click handler (`_toggle_card_log`) and an expanded-cards fetch pass were added to `__init__.py`'s `reload`; the toggle uses `asyncio.create_task` (a returned coroutine isn't reliably awaited). New `WARMING__CARD_LOG_LIMIT` config (default 30). Verified live in an isolated demo instance via Claude-in-Chrome. 622 tests green; ruff/ty/aislop clean.
- Second warming audit — 13 findings fixed (branch `fix/warming-audit-findings`) — **Done**: П1 rail relabelled "furthest step reached" (monotonic clamp kept by design); П2 fleet `max_daily_actions` override retired — per-account auto cap (phase + trust) is the single source of truth, `_BoardContext.max_daily` plumbing removed, field deprecated in schema; П3 mid-cycle readiness gate (`_transitions._gate_readiness`) parks degraded `active`/`sleeping` accounts to `error` (skips flood_wait/quarantine, CAS-guarded, mirrors `reconcile_warming_runtime`); П4 same-column drag is a no-op (`_BoardContext.drag` records the source column); П5 spam badge surfaces being-checked / probe-error text+colour+tooltip instead of a generic button; П6 `start_warming` clears stale `last_action`/`last_channel`; П7 `started_at` preserved when restarting an already-warming account; П8 pipeline detail uses `_ru_event(...)`; П9 no-channels skip no longer bumps `cycles_completed`; П10 quarantine chip is amber «история карантинов: N» off-state, red only when actively quarantined; П11 DM gated by trust band {excellent, good, watch} + readiness (threaded via `WarmingCycleRequest.dm_allowed`, folded into `compute_intensity`); П12 readiness text names spam-limit + critical-trust; П13 chat toggle disabled + note without a Gemini key. Review pass added the missing `cycle_not_ready` event translation. 642 tests green; ruff/ty/vulture/radon/aislop/deptry/bandit clean.
- Global errors/warnings feed (same branch) — **Done**: the board's bottom «Живая активность» feed is repurposed into «Ошибки и предупреждения · по всем аккаунтам» — it now shows only WARNING/ERROR rows aggregated across every account (the success "what happened" stream lives per-card now). New `LogFilter.problems_only` flag → `core/repositories/logs.py` adds `where(status != "success")`; `features/warming/_activity.py` queries `problems_only=True`, keeps the warming-relevance prefix filter **always-on** (the old «только прогрев» toggle removed as redundant on the warming page — excludes proxy/account-CRUD errors), and the feed is **collapsible** (click-to-hide header, same chevron UX as the per-card «Логи аккаунта»). Errors already get logged via `log_event`, so nothing is "duplicated" — just filtered. Rows render in **plain Russian** (no raw event names / JSON): `_termlog._event_label` (+ a flood-suffix rule) gives the label, `_termlog._humanize_detail` gives the reason (reuses `_board_checks._ru_reason` for `reasons`, translates the common Telethon errors → «канал не найден» / «сессия использовалась с двух устройств» / «ждём N с»); the per-card panel shares the same helpers. Tested in `tests/services/test_logs.py` + `tests/features/test_termlog.py`. 632 tests green; ruff/ty/aislop clean.
- Neurocomment data layer (issue #114, branch `feat/neurocomment-data-114`) — **Done**, built test-first. New `schemas/neurocomment.py` (campaigns, channel/account links, linked-group, readiness, comment records — Pydantic, no raw dict/list at boundaries) + `core/repositories/neurocomment/` package, split into `_tables`/`_campaigns`/`_comments` to stay under the aislop 400-line file budget (tables moved out of `core/db.py`, which returns to ~348 lines). Idempotent migration **#11** (`add_neurocomment_tables`) creates the six tables and the **partial unique index** `… WHERE active = 1` that enforces "a channel is in exactly one active campaign" at the DB level; the comment idempotency-claim is composite PK `(channel, post_id)` via `INSERT … ON CONFLICT DO NOTHING`. New `settings.neurocomment.*` namespace (reply 3–10s, joins 30–60s, ~10/h, ≤30 words, per-channel/day cap, retries ≤2), mirrored in `.env.example`. 9 repo tests (happy-path, channel uniqueness incl. FK-vs-conflict, account m2m, claim + outcome); 100% branch coverage on the new modules; full suite **653 green**; ruff/ty/aislop clean. The Ф0 throwaway prototype was removed (its job — proving `comment_to` — is done).
- Neurocomment reaction **engine** (issue #118, branch `feat/nc-engine-118`, built on #114 data + #115 actions + #116 listener + #117 onboarding) — **Done**, test-first. New `services/neurocomment/engine.py` = the on-post pipeline `handle_new_post(event)`: map post → active campaign (`fetch_active_campaign_for_channel`), filter (forward / media-no-caption / empty / link-only), select a ready+healthy+under-quota+non-cooled account (reuses `warming.pacing.evaluate_readiness` as the health gate, hourly + per-channel-day caps), win the atomic `claim_comment` (idempotency, no dup comments across concurrent events/restarts), generate via Gemini with a "≤ N words" instruction, light-check (word-count + `is_acceptable` + `try_reserve_sent` dedup, regenerate ≤ `max_retries`), human-pause `asyncio.sleep`, post `CommentOnPost`, classify (ok → `mark_comment_posted`; flood/slow/peer_flood → in-memory cooldown + `mark_comment_failed` + `release_sent_text`; gate errors → lazy-captcha readiness flip; other → failed). Whole pipeline wrapped so a fault is logged, never killing the listener. `services/neurocomment/_runtime.py` owns fire-and-forget per-post tasks (tracked set), `reconcile_neurocomment_runtime(listener_account_id)` subscribes/stops via the watch set, `shutdown_neurocomment_runtime` cancels with a timeout. `services/neurocomment/_state.py` = transient in-memory cooldown map (not persisted — single-process). New repo helpers (`core/repositories/neurocomment/`): `count_account_comments_since`, `count_account_channel_comments_since`, `fetch_active_campaign_for_channel`, `list_active_watch_channels` (+ `ChannelList` schema). `NewPostEvent` gained `is_forward`; listener populates it from `message.fwd_from`. New config `NEUROCOMMENT__PEER_FLOOD_COOLDOWN_SECONDS` (mirrored in `.env.example`). `_seams` now also exposes `generate_text` + `refresh_spam_status`. Full suite **723 green**, branch coverage ≥ 90% (engine/state 100%, runtime 98%); ruff/ty/aislop clean. **Out of scope → #119**: the NiceGUI page, `main.py` registration, listener-account selection UX. # ponytail: a flood/peer-flood drops the lost post (no requeue); per-event tasks are unbounded (semaphore later if volume grows).
- Neurocomment **UI + runtime wiring** (issue #119, branch `feat/nc-ui-119`) — **Done**, merged. New `features/neurocomment/` NiceGUI page (`_page.py`) + `services/neurocomment/board.py` (UI-agnostic board state, plain-Russian labels); `main.py` registers the page and reconciles/​shuts down the neurocomment listener runtime alongside warming; runtime state is persisted via new `core/repositories/neurocomment/_runtime.py` + migration **#12**; domain docs added in `.mex/context/neurocomment.md`. ruff/ty/aislop clean; CI green.
- **Ф1 neurocomment (#114–#119) is fully merged to `main`** (2026-06-23) via the six stacked PRs #122→#127 — squash-merged bottom-up, each CI-green (lint/ty/pytest-strict/aislop/semgrep/pip-audit). The #124↔#126 `is_forward` add/add (squash erases the shared #116 history) resolved trivially: engine's listener/schema/test files are byte-identical to the merged listener, so #126 landed as the verified engine-only diff. **Live canary on 1–2 accounts is intentionally NOT auto-run — it is an operator HITL step.** Входная капча → spike #120 (Ф2).
- **Ф2 start — #129 neurocomment quick wins** (branch WIP, 2026-06-23) — **Done (code)**, test-first. Five review-minors verified against `main` and fixed: (1) slow-mode now cools only `(account, channel)` — `services/neurocomment/_state.py` cooldown re-keyed to `(account_id, channel|None)`; engine routes `slow_mode_wait`→channel scope, keeps `flood_wait`/`peer_flood`/`premium_wait` account-wide (deviation from plan: `premium_wait` left account-wide — a non-premium account fails in *every* premium-gated chat, so account-wide is correct & keeps the existing test); (2) `msg_id=0` sentinel → pass `result.message_id` as-is (`int|None`), `mark_comment_posted` signature widened; (3) `_mark_comment` terminal-state guard (`status NOT IN (posted,failed)`) — idempotent, late failure can't unposted a posted claim; (4) `NeurocommentSettings` `@model_validator` min≤max for reply/join delays; (5) UI listener dropdown refresh via `set_options` in `on_toggle` (smaller than the planned `@ui.refreshable` — only the dropdown was stale). `#129` minor (ClickButton ok-on-no-match) deferred into spike #120 (action dormant — used by tests only). New tests in `tests/services/test_neurocomment_engine.py`, `tests/core/test_neurocomment_db.py`, new `tests/core/test_config.py`. Committed on branch `fix/nc-129-quick-wins` → **PR #130** (open, against `main`). Post-open adversarial audit (3 parallel reviewers: cooldown logic / DB idempotency / types-tests-callsites) found **no real bugs**; two minors fixed in a follow-up commit: (a) `_state.in_cooldown` now lazily evicts expired keys (bounds the cooldown dict; residual cold-key growth left as a `# ponytail:` ceiling) and de-dups the `channel=None` double-lookup via a set; (b) `mark_comment_posted`/`mark_comment_failed` docstrings now state the idempotent-guard return contract. 767 tests green; ruff/ty/aislop clean; coverage ≥ 90%. **Merged to `main` via #130.**
- **Ф2 #131 — deletion-sweep → escalating channel back-off** (PR #135, 2026-06-23) — **Done, merged to `main`**, test-first. Periodic asyncio **sweep** (`services/neurocomment/_runtime`) re-reads each active channel's recent posted comments via new read action **`CheckMessagesAlive(channel, message_ids) → missing_ids`** (`core/telegram_client/_read`: resolves the linked discussion group, `get_messages` `None` = a gone comment). `missing ≥ channel_backoff_min_deletions` trips an **escalating in-memory channel cooldown** in `_state` (`min(base·2^trips, max)`, no re-trip while already cooled, self-healing on restart) + a WARNING on the operator feed; `engine._handle_new_post` skips a backed-off channel before account selection. Sweep owned by the runtime, started/stopped with the listener; five `settings.neurocomment` knobs (`deletion_sweep_*`, `channel_backoff_*`) mirrored in `.env.example` (`interval=0` disables). 782 tests green; ruff/ty/aislop/bandit/vulture/radon/deptry clean; coverage ≥ 90% (engine & `_state` 100%, `_runtime` 94%).
- **Ф2 #132 — cross-account semantic dedup** (branch `feat/nc-f2-semantic-dedup`, 2026-06-23) — **Done (code)**, test-first via `/tdd`. New pure `services/content.similarity(a, b)` = token-set Jaccard over `normalize_text` (intersection size / union size; two empty token sets count as identical), unit-tested. Plugged into the `_generate_acceptable` retry loop **after** the exact-hash `try_reserve_sent` claim (which stays the atomic gate): the new helper `_recent_channel_comments` fetches the channel's recent **posted** comment texts in the window once via the existing `list_posted_comments_since` (campaign-scoped query, filtered to the posting channel) — a candidate whose max similarity ≥ threshold is released (`release_sent_text`) + regenerated. Two new knobs in `settings.neurocomment`, mirrored in `.env.example`: `semantic_dedup_threshold` (default **0.0 = off**, opt-in; range 0–1) and `semantic_dedup_window_hours` (default 24). No new gateway/dep, no migration — group+window scoped, channel-local. Tests: `similarity` unit + engine near-dup-reject+regen / distinct-accepted / threshold-0-disables / channel-scoping. 772 tests green; ruff/ty/aislop clean; coverage ≥ 90% (engine.py 100%, `similarity` 100%). **Merged to `main` via #134.**
- **Ф2 #136 — UI управление кампаниями** (branch `claude/peaceful-bhabha-e08f72`, 2026-06-23) — **Done (code), НЕ в `main`** (своя ветка, мерж по согласованию с оператором), test-first via `/grill-with-docs` → `/tdd`. Закрыты два пробела на `/neurocomment`: **(1) un-assign аккаунта** — новый repo `remove_account_from_campaign(campaign_id, account_id)` в `core/repositories/neurocomment/_campaigns.py` (hard DELETE строки m2m `neurocomment_campaign_accounts`; scoped по паре, идемпотентно при отсутствии, **без каскада** — `neurocomment_readiness` шарится по `(account, channel)` между кампаниями, каскад поломал бы соседнюю), re-export через `__init__` + `core.db`; ветка снятия чекбокса в `_render_account_picker.on_toggle` зовёт repo напрямую (тонкий CRUD без сервиса — сложившаяся конвенция фичи, как `assign`/`deactivate`; grill-решение: pass-through-сервис был бы shallow-модулем). **(2) Мультикампанийная страница** — `render_neurocomment_page` заменил хардкод `campaigns[-1]` на `ui.select`-переключатель (все статусы) + `@ui.refreshable`-секцию (setup + work-view пере-рендерятся при смене, по умолчанию первая кампания); новые чистые тестируемые хелперы `campaign_status_label` + `_CAMPAIGN_STATUS_RU` (зеркало `_CHANNEL_STATUS_RU`) и `campaign_options(CampaignList)->dict`. `pyproject.toml`: `features/neurocomment/**` добавлен в RUF001/2/3 per-file-ignore (как accounts/logs/warming — кириллица в UI намеренная). Новых config-ключей не потребовалось. Пост-ревью 3 параллельных субагента (логика-repo / NiceGUI-lifecycle / конвенции-тесты) — без blocker/major (scope-тест mutation-проверен как реальный guard); убраны две минорные избыточности, обе подтверждены ревьюерами: per-switch накопление `on_disconnect` (таймер NiceGUI client-scoped — сам отменяется на clear/disconnect, manual teardown + `context`-импорт удалены) и избыточный listener-null guard (его делает `set_options`). 796 тестов зелёные; ruff/ty/aislop/format чисто; branch-cov ≥ 90% (`_campaigns.py` 100%, новые хелперы 100%). Issue #136 (milestone «Ф2 · Масштаб», `ready-for-human` — код готов, ждёт ревью/мержа оператором). **Пост /code-review (max-effort, 10 finder-углов + source-verify):** две «громкие» находки опровергнуты чтением исходников NiceGUI 3.13 — `set_options`→`update`→`_update_options` сам обнуляет stale-value для single-select (`self.value = before_value if before_value in self._values else None`), поэтому удалённый listener-guard действительно лишний; `on_change=section.refresh` пробросил бы change-event в 0-арг `section()` → `on_switch`-wrapper нужен. Исправлены 2 минорных: дефолт переключателя возвращён на новейшую кампанию (`campaigns[-1]`, как было до диффа), восстановлен явный `on_disconnect`-teardown таймера (как во всех 4 соседних poll-сайтах + исходный код — «match existing style»). 1 major (altitude): per-campaign Start/Stop/listener поверх **fleet-global** слушателя (рантайм — один слушатель на объединение каналов всех активных кампаний). **Исправлено в этой же ветке (коммит `048c49a`, Closes #137):** listener + Запустить/Остановить вынесены в единый `_render_runtime_controls()` (рендерится один раз, listener выбирается из всех аккаунтов), независимо от переключателя; per-campaign секция оставляет каналы + назначение аккаунтов + онбординг; `on_toggle` упрощён (исчезла связка listener-sync), временный caption убран. Два коммита на ветке `claude/peaceful-bhabha-e08f72`: `451f824` (#136) + `048c49a` (#137); PR против `main` открыт, мерж — за оператором. **(Смержено в `main` как PR #138.)**
- **Shared top-nav extracted** (branch `fix/nav-shared-component`, 2026-06-23) — **Done**, test-first. The cross-page nav was hand-rolled in all four page headers (`features/accounts/_header.py`, `features/warming/__init__.py`, `features/logs.py`, `features/neurocomment/_page.py`); the neurocomment page (added in #119) was linked only from its own header, so it was reachable by URL but missing from the menu on every other page. New **`features/shared/`** — the single sanctioned cross-feature UI namespace — holds `nav.py` = `NAV_LINKS` (one source of truth) + `render_nav(active)` + `TOP_BAR_CLASSES`; all four headers now call `render_nav(route)`, so a new page is one tuple in `NAV_LINKS` and shows up everywhere. `tests/test_architecture.py::test_no_cross_feature_imports` updated to allow `features.shared` (documented in `context/conventions.md` #1); regression test `tests/features/test_nav.py` asserts every page route is present in `NAV_LINKS`. 799 tests green; ruff/ty/aislop/vulture/deptry/radon/bandit clean.
- **Neurocomment scale optimization + architecture firewall** (branch `claude/cool-chaum-f870ec`, 2026-06-23) — **Done**, test-first via `/grill-with-docs`; merged via **PR #143**. `engine._select_account` rewritten to one bulk signal load + pure in-memory scoring (was ~7 DB reads/candidate → flat in fleet size); spam read **cache-only** on the post path with a one-shot probe moved to onboarding (`_probe_account_spam`); bulk `GROUP BY` quota readers + a channel-scoped recent-comments read in new `core/repositories/neurocomment/_quota.py`; migration **#13** adds three composite indexes (EXPLAIN-verified, two covering); story-image normalize offloaded via `asyncio.to_thread`. **Rules:** `tests/test_architecture.py` enforces a features→core **allowlist** (`config`+`logging`) — caught + fixed the `_page.py`→`core.db`/repository drift behind a new `services/neurocomment/campaigns.py` seam (`link_channel` returns a typed `ChannelLinkOutcome`); new pre-push `arch-guard` hook. 806 tests green; all gates clean; coverage ≥ 90%. Two ADRs in `context/decisions.md`.

- **Ф2 #144 — challenge-solver foundation: state split + migration #14** (branch WIP, 2026-06-24) — **Done (code)**, test-first via `/tdd`. Splits the single neurocomment `captcha_gated` pair-state into three: **`chat_restricted`** (Telegram-level write block — unsolvable, solver never runs), **`bot_challenge`** (guardian-bot inline challenge — solvable; set by the solver in a later slice), **`bot_challenge_backoff`** (channel-derived cooldown — counter lands in #4). Migration **#14** (`add_neurocomment_challenges`) creates the audit-and-cache table `neurocomment_challenges` (`id, challenge_hash, account_id, channel, raw_text, button_labels_json, decision_json, outcome DEFAULT 'pending', decided_at, outcome_at`) + indexes `ix_nc_challenges_hash_outcome` (cache fast-path) & `ix_nc_challenges_account_channel_decided` (engine outcome resolution) + `neurocomment_campaigns.solver_enabled BOOLEAN DEFAULT NULL` (per-campaign override of the global flag). **No data remap needed**: channel status is *derived* from `neurocomment_readiness` booleans, never stored, so the conservative `captcha_gated→chat_restricted` remap is purely the `board._channel_status` derivation change (covered by `test_channel_status_chat_restricted`). `onboarding._classify_join` maps `_GATE_ERRORS`→`chat_restricted`; `schemas.neurocomment` `OnboardingState`/`ChannelStatus` literals updated; `features/neurocomment/_page.py` renders three distinct RU badges via a new `_CHANNEL_STATUS_ICON` map + `channel_status_icon()` helper. `bot_challenge`/`bot_challenge_backoff` are in the enum + UI map but **not yet derived** — the solver that sets them lands in #146. No new config knobs (the global `challenge_*` flags arrive with the solver slice). 815 tests green; ruff/ty/aislop/bandit/vulture/radon/deptry clean; coverage ≥ 90%. Issue #144 (milestone «Ф2 · challenge solver»). **Merged to `main` via PR #150.**
- **Ф2 #145 — WaitForBotChallenge + detection-only solver** (branch WIP, 2026-06-24) — **Done (code)**, test-first via `/tdd`. Empirical-foundation slice: the solver now *detects* guardian-bot challenges at onboarding and records them, but **does not call Gemini** (always `give_up`). New gateway read-action `WaitForBotChallenge(chat_id, timeout_seconds) → BotChallengeWaitResult(message: BotChallengeMessage | None)` in `core/telegram_client/_read_challenge.py` (split out of `_read.py` for the file-size budget): a short-lived `events.NewMessage` subscription on the just-joined group, resolved-entity (not bare `linked_chat_id`), under `asyncio.wait_for`; the pure, unit-tested predicate `_extract_bot_challenge` matches sender-is-bot ∧ `ReplyInlineMarkup` ∧ addressed-to-us (mention / `tg://user?id=` entity / reply-to-join), biased false-negative > false-positive. New `services/neurocomment/challenge.py` `solve_if_present(account_id, channel, group_id) → {"no_challenge","give_up"}` (Gemini-free stub) + `_challenge_hash = sha256(normalize_text|sorted(labels))`. New repo `core/repositories/neurocomment/_challenges.py`: `insert_challenge(ChallengeInsert)`, `list_failed_for_channel` (drill-down), `list_challenged_channels` (board signal). `onboarding._classify_join` ok-branch now calls the solver (`group_id` threaded through; `_channel_comments_enabled`→`_resolve_group_for_join` returns `int|None`): `no_challenge`→`ready`, `give_up`→`bot_challenge`. `board._channel_status` distinguishes `bot_challenge` (a challenge row exists) from `chat_restricted` (no row) — both share the joined-but-not-ready readiness shape. UI: per-channel lazy drill-down expansion (raw text + button labels via `list_channel_challenges`, loaded on expand so the 4 s poll stays cheap) + tested `challenge_summary` helper. New `schemas/challenge.py` (BotChallengeMessage / ChallengeInsert / ChallengeRow / ChallengeRowList / ChallengedChannels). 1 new config knob `challenge_wait_timeout_seconds` (20.0), mirrored in `.env.example`. Detection runs unconditionally (no global flag yet — that lands with the Gemini solver #146), so onboarding pays the wait once per pair to gather the empirical capture set. 836 tests green; ruff/ty/aislop/bandit/vulture/radon/deptry/format clean; coverage ≥ 90%. **/code-review** (max-effort, 2-angle fan-out): one real finding fixed (bare `chat_id` → resolve the group entity before subscribing, matching the `CheckMessagesAlive` pattern); sender-None is the intended false-negative. Issue #145 (milestone «Ф2 · challenge solver»), `Blocked by: #144` (merged). **Merged to `main` via PR #151.**
- **Ф2 #146 — solver: Gemini responseSchema + cache + click** (branch WIP, 2026-06-24) — **Done (code)**, test-first via `/tdd`. Real text/button captchas now solve end-to-end. `core/gemini.GeminiRequest` gains `response_schema_json: dict | None` → server-side `generationConfig.responseSchema` + `responseMimeType` (parse-fails effectively impossible). New `schemas/challenge.ChallengeDecision` (`action ∈ click_button/send_text/give_up` + `button_index` + `text` + `confidence 0..1` + `reasoning ≤200`) with validators. `services/neurocomment/challenge.solve_if_present` is now the full pipeline: detect → **image early-exit** (`has_photo` → give_up, no Gemini) → **cache lookup** (`lookup_cached_decision` by hash, cross-account, solved-only) → **Gemini** (`_seams.generate_text` with the decision schema, `asyncio.wait_for` `challenge_gemini_timeout_seconds`; timeout/error/parse-fail → give_up) → **log-normal humanize** (`_human_delay_seconds`, reuses warming's lognorm shape, clamped to `challenge_click_delay_*`) → **dispatch** `ClickButton` / `PostComment` → audit `pending` with `decision_json`; returns `solved`/`failed`/`give_up`/`no_challenge`. `_challenges` repo gains `lookup_cached_decision` + `resolve_pending_outcome`. `engine._classify_post` resolves the pair's pending row → `solved` on first ok comment, `failed` on gate error (ground-truth verification, no extra wait). `onboarding._solve_and_record`: `give_up`/`failed` → `bot_challenge`, `no_challenge`/`solved` → `ready` (optimistic; engine confirms). 4 config knobs (`challenge_gemini_timeout_seconds` 10, `challenge_click_delay_min/max_seconds` 3/6 with min≤max validator, `challenge_min_confidence` 0.7 reserved Phase-2), mirrored in `.env.example`. `engine.py` comments trimmed to stay under the aislop file-size budget after the +import/+2 calls. 862 tests green (+26); ruff/ty/aislop/bandit/vulture/radon/deptry/format clean; branch coverage ≥ 90%. **/code-review** (max-effort, 2-angle): conventions clean; one benign edge flagged (`# ponytail:` re-onboard can orphan a prior pending row — feeds neither board nor cache). Issue #146 (milestone «Ф2 · challenge solver»), `Blocked by: #145` (merged). **Merged to `main` via PR #152** (CI caught a missing-GEMINI-key crash — `_gemini_decision` now gives up on a keyless deploy + test fixtures set a key; follow-up commit).
- **Ф2 #147 — per-channel back-off on K solver failures** (branch WIP, 2026-06-24) — **Done (code)**, test-first via `/tdd`. Clones the #131 deletion-back-off shape: `_state.register_challenge_failure(channel, now, min_failures, base, max)` counts solver click-failures (`pending → failed`) and, on the K-th, trips an escalating in-memory cooldown (`base * 2^trips`, capped at max; self-healing on restart) + `is_channel_in_challenge_backoff`. `engine._handle_new_post` skips a backed-off channel (alongside the deletion back-off) before selection; the gate-error branch calls `register_challenge_failure` only when `resolve_pending_outcome` actually resolved a pending row (now returns `bool`), and logs a WARNING **once per trip**. `onboarding._join_and_classify` leaves a backed-off channel alone (no join, no solver) → `bot_challenge_backoff`. `board._channel_status` renders `bot_challenge_backoff` (precedence after comments_off, before ready) from the in-memory state. 3 config knobs (`channel_challenge_backoff_min_failures` 3 / `_base_seconds` 3600 / `_max_seconds` 86400, base≤max validator), mirrored in `.env.example`. **engine.py was at the aislop file-size ceiling**, so the pure post-filter (`_filter_reason`/`_is_link_only`) was extracted to a new `services/neurocomment/_filters.py` (`filter_reason`, behaviour-identical) to make room. 873 tests green (+11; new `tests/services/test_neurocomment_state.py` for escalation/cap/self-healing + engine/onboarding/board integration); ruff/ty/aislop/bandit/vulture/radon/deptry/format clean; branch coverage ≥ 90%. **/code-review** (max-effort): no findings (escalation math matches #131; extraction line-for-line identical; idempotency correct). Issue #147 (milestone «Ф2 · challenge solver»), `Blocked by: #146` (merged). **Merged to `main` via PR #153.**
- **Ф2 #148 — operator UI loop + opt-in solver flag** (branch WIP, 2026-06-24) — **Done (code)**, test-first via `/tdd`. Closes the «вижу → понимаю → действую → контролирую» loop and makes the solver **opt-in**. Global `settings.neurocomment.challenge_solver_enabled` (default `False`) + per-campaign `neurocomment_campaigns.solver_enabled` (NULL/TRUE/FALSE; column from #14, now in the SQLAlchemy table def + `NeurocommentCampaign`/board schema). `onboarding._effective_solver_enabled` (campaign override beats global; both default off) gates `solve_if_present` — threaded keyword-only through the join chain. Migration **#15** adds `neurocomment_readiness.human_skipped`; `mark_human_skipped` sets it + `ready=0` (engine excludes via the existing ready gate — no new selection logic), `delete_readiness` powers retry. New repo readers: `count_by_outcome(channels, since) → ChallengeOutcomeCounts` (header counters), `reasoning` parsed from `decision_json` into `ChallengeRow` (drill-down). `challenge.retry_pair(account_id, channel)` erases readiness + re-onboards (re-runs the solver). `_campaigns.update_solver_enabled`; thin service seams `set_solver_enabled` / `skip_pair` / `count_challenge_outcomes` (firewall). UI: per-campaign solver switch (follow/on/off) + 4 outcome counters with a Сегодня/7 дней/Всё-время toggle + Retry/Skip buttons per failed-row in the drill-down — the work-view render was extracted to a new `features/neurocomment/_workview.py` (engine-of-the-page) to keep `_page.py` (now setup + tested label helpers) under the aislop file-size budget; `_page` lazily imports `render_work_view` to avoid the helper-import cycle. 891 tests green (+18); ruff/ty/aislop/bandit/vulture/radon/deptry/format clean; branch coverage ≥ 90%. **/code-review** (max-effort, 2-angle): correctness clean (gating truth table, human_skipped-via-ready, retry, counters, migration #15, reasoning, the split — all verified); one finding applied (File Map gains `_workview.py`). Solver switch sits in the work-view control panel (next to the counters it affects) rather than the static setup section — deliberate (the board carries `solver_enabled`). Issue #148 (milestone «Ф2 · challenge solver»), `Blocked by: #146` (merged). **This completes the Ф2 challenge-solver build (#144–#148); the live HITL canary #149 is operator-run, not touched.**

- **Neurocomment page redesign** (branch `feat/nc-page-redesign`, 2026-06-24) — **Done (code)**, grilled via `/grill-with-docs` then built. Full visual rework of `/neurocomment`: compact, thought-through layout + heavy animation that lets the operator *see* the engine is live. New **`features/neurocomment/_engine_panel.py`** = the animated hero card: a six-step pipeline rail (Слушаю → Новый пост → Аккаунт → Генерация → Публикация → Контроль, mirroring `engine._handle_new_post`) that, while running, flows its connectors + pulses its nodes in a wave, and plays a one-shot **launch cascade** on Start (pure CSS, `tb-nc-*` keyframes in `__init__.py` via `ui.add_css(shared=True)`, `tb-nc-` prefixed to stay isolation-clean — no `features/warming` import); a live status ticker (blinking dot + «Слушаю N каналов · последний коммент X назад»); and four fleet counters (за час / сегодня / готовых аккаунтов / каналов готово) where «сегодня» flashes on tick-up. The rail is a `@ui.refreshable` refreshed only on a running-flip (so the cascade plays once per Start); the ticker+counters refresh every 4 s. New static **`_explainer.py`** «Как работает» card (mirrors warming's `_render_how_it_works`, driven by the shared `PIPELINE_STEPS`). Config UI moved to **`_setup.py`** as collapsible `ui.expansion` blocks with channels as removable chips. The running-state signal is a new thin service **`neurocomment_runtime_status() → NeurocommentRuntimeStatus(running, active_channels, listener_account_id)`** (`services/neurocomment/_runtime.py`, re-exported) reading the persisted listener id — required because the features→core firewall (#143) blocks the page from reading `core.db` directly. New pure tested helpers in `_page.py` (`PIPELINE_STEPS`, `fleet_activity`, `relative_time`, `runtime_status_text`) + the new schema, covered in `tests/features/test_neurocomment_labels.py` / `tests/services/test_neurocomment_runtime.py`. No engine/runtime behaviour change; no new config keys (so no `.env.example` work). 905 tests green; ruff/ty/aislop/vulture/radon/bandit/format clean; coverage ≥ 90%. **/grill-with-docs → /ponytail-review → /code-review** (high-effort, 3-angle fan-out): no correctness bugs, no regressions, firewall + Pydantic boundary intact; applied a `reload()` shrink (whole-object `state` rebind) and documented the engine-panel/work-view double board-load as a deliberate `# ponytail:` trade-off (sub-ms local SQLite; merge timers only if it bites).

- **Neurocomment page redesign — round 2** (branch `feat/nc-page-fixes`, 2026-06-24) — **Done (code)**, planned in Plan Mode + grill answers, then built. Fixes five issues from live testing of PR #156: **(1) Flicker** — the work view and engine panel rebuilt their DOM every 4 s; added pure tested signature gates `board_content_signature` / `live_signature` in `_page.py` (mirrors warming's `_card_signature`), so `render_work_view.reload()` and `_engine_panel.reload()` only `.refresh()` on a real content change (the ticker's «X назад» clock is bucketed → ≤ once/min, no blink). **(2) Warmed-accounts field** — new global read-only «Прогретые аккаунты» card at the very top (chips of accounts warmed ≥ `settings.neurocomment.warmed_min_days`, default 14), backed by a new thin service `services.warming.list_warmed_accounts(min_days)` over `load_board` + `WarmedAccount`/`WarmedAccountList` schemas. **(3) Layout** — `_page.py` reordered: warmed field → campaign switcher → a side-by-side row [«Новая кампания» (global, static) | «Настройка» (per-campaign refreshable)] → work section; split the old single `section()` into two refreshables both refreshed by `on_switch`. **(4) Minimalist «Капчи»** — `_workview._render_solver_controls` collapsed from a card + 4 badges to a thin strip (dense selects + one inline `✓·✗·⊘·⏳` label via `set_text`). **(5) Collapsible logs** — new `features/neurocomment/_logpanel.py` (event→RU map + pure tested `nc_event_label`/`nc_log_detail` + `LogPanelState` + signature-gated `refresh_logs` + `render_log_panel`) mounted in the engine card; needed a new `LogFilter.event_prefix` (schema + one repo WHERE `event.like('neurocomment%')`) since neurocomment listener/sweep rows aren't account-scoped; `tb-nc-log*` dark-terminal CSS re-stated in `__init__.py` (no `features/warming` import). 2 config knobs (`warmed_min_days`, `log_limit`) mirrored in `.env.example`. No engine/runtime behaviour change. 924 tests green (+19); ruff/ty/aislop/vulture/radon/bandit/format clean; coverage ≥ 90%. **/ponytail-review → /code-review** (high-effort, 2-angle): firewall + conventions clean; fixed one real finding (the `neurocomment_channel_backoff` log row showed only the cooldown, dropping the `missing` deletion count — `nc_log_detail` now composes «удалено N · пауза M с») and closed one signature gap (`max_comments_per_hour` added to `board_content_signature`).

- **Neurocomment page — round 3 (trust & safety)** (branch `fix/neurocomment-trust-safety`, 2026-06-25) — **Done (code)**, planned in Plan Mode after verifying a ~30-item live-testing UX audit against the code (many flagged items already had tooltips / conditional behaviour and were left untouched). Scope «trust & safety first»: **(A) Start-gate consistency** — `_engine_panel.on_start` now refuses via the new pure `start_block_reason(ready_accounts, *, has_listener)` (`_page.py`, tested) when there is no listener or zero ready accounts, and the idle ticker names the real blocker, so the «нет прогретых аккаунтов» warning and the actual behaviour agree (was: start silently no-ops). **(B) Listener dropdown** filtered to accounts with a live session via new thin service `services.accounts.list_listener_accounts()` (reuses `health_for_status == "ok"`; returns `AccountList`), with an empty-state hint. **(C) Онбординг** button now shows an immediate toast + Quasar `loading` spinner (try/finally) instead of 4 s of silence. **(D)** Fleet-wide «Остановить» now asks for confirmation via a `ui.dialog` (mirrors `features/accounts/_dialogs._open_delete_dialog`). **(E)** Four genuinely-missing legends/tooltips added: Капчи `✓·✗·⊘·⏳` strip (`_workview`), «от N дн» warmed-field hint (`_setup`), and the warming-card DM chip + phase chip (`features/warming/_board.py`). No engine/runtime behaviour change; no new config knobs (listener-eligibility reuses `health_for_status`). 930 tests green (+2 new: `start_block_reason`, `list_listener_accounts`); ruff/ty/aislop/vulture/radon/bandit/deptry/format clean; coverage ≥ 90%.

- **Full dashboard redesign — `Telebuba.dc.html` design system** (branch `redesign/dashboard-dc`, 2026-06-28) — **Done (code)**. Design imported from the claude.ai/design project via its `GetFile` RPC through the logged-in browser (headless MCP auth unavailable) → 539-line spec in scratchpad. New **global design system** under `features/shared/`: `theme.py` (`apply_theme` — Inter + JetBrains Mono web fonts, `#0066FF` Quasar primary) + `theme.css` (the design's verbatim global stylesheet: warm `#F1EFED` canvas + `tb-*` animation/utility classes) + `components.css` (reusable vocabulary — buttons/cards/badges/segmented/inputs/stat-cards/terminal/toggles). `nav.py` reworked from plain text links into the design's sticky global top bar (logo, five links incl. the new **Настройки**, «Система активна» badge, bell, avatar) + a `page_shell(active)` context manager every page now opens; pure tested `nav_link_html`; legacy `TOP_BAR_CLASSES` removed once all pages migrated. New **Настройки** page (`features/settings.py` + static `settings.html`; client-side eye-toggle/switches/«Сохранено», **persistence intentionally deferred** to a follow-up task — the design's save is a mock). All five pages (Аккаунты / Прогрев / Нейрокомментинг / Логи / Настройки) restyled to the spec by parallel subagents; the aislop 400-code-line budget kept by extracting `features/accounts/_table_cells.py`, `features/warming/_board_header.py`, `features/neurocomment/_labels.py`. **Visual-only — no business logic / service / schema / runtime / data-flow change.** 975 tests green; ruff / ty / aislop (151 files, 0) / vulture / radon / bandit / deptry / semgrep / pip-audit clean; branch coverage 92.27%. PR opened against `main`; per-page live renders sanity-checked on isolated throwaway instances.

- **Redesign pivot — serve the design SPA verbatim; NiceGUI UI deleted** (branch `redesign/dashboard-dc`, 2026-06-28) — **Done (code); SUPERSEDES the NiceGUI re-implementation entry above.** Live testing showed the hand-built NiceGUI pages didn't match the design (broken non-full-width header + general drift), so per the operator the design's own code is now the frontend. `web/index.html` (the canonical `Telebuba.dc.html` from the `claude_design` MCP, omelette-instrumentation stripped) + `web/support.js` (the design-canvas **`dc` runtime**) are served verbatim: `main.py` serves `/`→index.html and `/support.js` via NiceGUI's `app.add_static_file` (no direct FastAPI dep), keeping only the warming/neurocomment startup/shutdown hooks + `ui.run`. **The entire `features/` NiceGUI UI layer and `tests/features/` were deleted.** Renders pixel-perfect standalone (verified on an isolated throwaway instance; the dc-runtime `new Function`-evals the design's logic class and needs `@babel/standalone` — loaded from CDN). `tests/test_architecture.py` dropped the now-moot features-layer checks; the `features` smoke imports in `test_canary`/`test_device_fingerprint` were removed. Vendored `web/` is excluded from aislop (the gate filters `web/` diagnostics — its `--exclude` is unreliable), semgrep (`.semgrepignore`), and pre-commit (top-level `exclude`). 714 tests green; coverage 93.96%; ruff/ty/aislop/vulture/radon/bandit/deptry/semgrep/pip-audit all clean. **Deferred (next step):** the SPA shows the design's MOCK data — wiring it to the live Python backend (real accounts/campaigns/logs + working actions) is not done; also the CDN deps (GSAP/Babel/fonts) should be vendored locally for offline/robust serving. The backend (`services`/`core`/`schemas`) is untouched and still runs the runtimes. NOTE: several `.mex/context/*` docs + non-negotiables still describe the old 4-layer NiceGUI UI (Feature Isolation, UI-thin, etc.) — now partially stale; a context-doc sweep is a follow-up.

- **Split-stack pivot — S1 #163: migrate `.mex` law to the React-SPA-over-FastAPI world** (branch `chore-163-mex-split-stack-docs`, 2026-06-28) — **Done (docs only, no code).** First slice of the 12-task migration (#163→#174) that replaces NiceGUI with a **React+TS+Vite SPA over a FastAPI `/api/v1` JSON API** (the 3 ADRs of 2026-06-28 in `context/decisions.md`). Rewrote the project law to describe the **split-stack target**: new top backend layer **`api/`** (UI-thin routes; imports only `services`/`schemas`/`core.config`/`core.logging`/`fastapi`) replacing `features/`; a separate **`frontend/`** tree (FSD). Files: **NEW `context/frontend.md`** (FSD layers app/routes/pages/widgets/features/entities/shared + one-directional import matrix + slice public-API `index.ts` rule + data-only-via-`shared/api` + TS-strict + i18n + tokens-in-tailwind.config + the FE gate set Steiger/eslint/prettier/tsc/Vitest≥80%/Playwright/gen-api-drift); rewrote `architecture.md` (System Overview, Layer Model, Folder Structure +`api/`+`frontend/`−`features/`, Allowed/Forbidden import matrices with the `api/` allowlist, cross-cutting API contract: error envelope `{error:{code,message,fields?}}` + `Page[T]` + locale-neutral + cookie auth); `conventions.md` (#1/#5/#11 → `api/` UI-thin; #3/#10 scoped to backend + `VITE_*` frontend-config rule; File Placement Guide + pre-commit checklist); `stack.md` (−NiceGUI, +FastAPI/uvicorn single-worker + frontend summary); `ci.md` (frontend jobs + gen-api drift check; coverage source `api/core/schemas/services`; removed the red-until-first-feature gotcha); `logging.md` (third tier → React Logs page over `/api/v1/logs`; event-code-for-i18n as staged refinement); `AGENTS.md` (Stack/File Map/12 non-negotiables, +locale-neutral rule); `ROUTER.md` (route to `frontend.md` + methodology re-pointed to the split stack). Replaced `patterns/add-feature.md` with **`add-api-endpoint.md` + `add-frontend-slice.md` + `add-auth.md`** (+ INDEX + fixed inbound edges). Swept residual stale `features/`/NiceGUI claims out of `services.md`/`setup.md`/`skills.md`/`warming.md`/`neurocomment.md`/`add-warming-job.md` (kept the superseded-ADR + "old features/ removed" references as intentionally historical). `npx mex-agent check` → **97/100** (only the by-design `TOOL_CONFIG_DRIFT` warning; the transient `MISSING_PATH: api/` errors were resolved by keeping `api/` out of ROUTER's inline path tokens — the package itself is built in #164, per the issue's own note that the arch-test re-point cannot reference `api/` before it exists). **Backend code (`api/`/`auth`) and `frontend/` do not exist yet** — they are built across #164–#174. PR opened against `main`; **operator review requested before merge** (highest-blast-radius slice). **Merged to `main` via PR #176.**

- **Split-stack pivot — S2 #164: FastAPI/uvicorn foundation, drop NiceGUI** (branch `feat-164-fastapi-foundation`, 2026-06-28) — **Done (code), built test-first.** Stood up the backend HTTP layer and removed NiceGUI; business logic in `services`/`core`/`schemas` untouched (additive). New **`api/`** package (UI-thin top layer): `api/__init__.py` `create_app(lifespan)` (CORS + error handlers + `/api/v1` router), `api/errors.py` (error-envelope handlers — raised `HTTPException`, pydantic 422 remap, and a catch-all `Exception`→500, all → `{error:{code,message,fields?}}`), `api/v1/{health,accounts}.py` seed endpoints (`GET /api/v1/health`→`HealthStatus`; `GET /api/v1/accounts`→`Page[AccountRead]`). New cross-cutting contract in **`schemas/api.py`**: `ErrorDetail`/`ErrorEnvelope`, generic `Page[T] = {items, next_cursor}` (PEP-695 `class Page[T]`), `HealthStatus`. New service seam `services.accounts.list_accounts_page(*, query, status, cursor, limit) → Page[AccountRead]` + `InvalidCursorError` (opaque offset cursor; fetch `limit+1` to detect a further page). **`main.py`** rewritten to the FastAPI/uvicorn composition root: module-level `app = create_app(lifespan)`, FastAPI **`lifespan`** runs warming + neurocomment reconcile/shutdown (+ telegram pool teardown last), `_mount_frontend` serves the SPA (`StaticFiles` for `/assets` + a catch-all returning `index.html`, API routes first), `uvicorn.run("main:app", host, port, workers=1)`. **NiceGUI removed**: dep dropped (+13 transitive deps gone from the lock), `_NiceGuiSlotDeletedFilter` + the stdlib `logging` import deleted from `core/logging.py`. **Config**: `settings.ui` (incl. `reconnect_timeout`) retired → new `settings.api` (host `127.0.0.1`/port/cors_origins/version) + `settings.auth` placeholder (`secret`); `.env.example` updated (`UI__*`→`API__*`+`AUTH__SECRET`). **`tests/test_architecture.py`** re-pointed: new `test_api_imports_only_allowlisted` (api may import only `services`/`schemas`/`fastapi`/stdlib + `core.config`/`core.logging`, via `sys.stdlib_module_names`); services now also ban `fastapi`/`api`; schemas ban `api`/`fastapi`; core bans `api`; `api`+`api/v1` added to layer/subpackage checks. **Gates**: coverage source/ty/vulture/deptry `+api`; `pyproject` deps `−nicegui +fastapi +uvicorn`; starlette CVE override comment updated (now via fastapi, not nicegui). New `tests/api/test_app.py` (health, page envelope, pagination cursor, invalid-cursor 400, 422 remap, unhandled→500). **723 tests green; api/ 100% branch coverage; ruff/ty/aislop/vulture/radon/bandit/deptry/semgrep/pip-audit clean; mex 97/100; app boots via `uvicorn main:app` with both routes present.** Frontend serving is transitional (serves `web/` until `frontend/dist` exists; #173 removes `web/`). `Blocked by #163` (merged). **Merged to `main` via PR #177.**

- **Split-stack pivot — S3 #165: OpenAPI → hey-api client-gen + CI drift gate** (branch `build-165-gen-api-client`, 2026-06-28) — **Done (code).** The contract pipeline (pydantic → FastAPI OpenAPI → typed TS client) plus the hard anti-drift gate. New minimal **`frontend/`** to host the generated client: `package.json` (devDeps `@hey-api/openapi-ts@0.95` + `prettier@3.9`, pinned via committed `package-lock.json`), `openapi-ts.config.ts` (input `openapi.json`, output `src/shared/api`, plugins `@hey-api/client-fetch` + `@tanstack/react-query`), npm script `gen:api` (openapi-ts + prettier). New orchestrator **`tools/gen_api.py`** (run as `uv run python -m tools.gen_api`): dumps the OpenAPI from the **pure `create_app()`** (SPA catch-all excluded — only `/api/v1` routes), writes `frontend/openapi.json` with forced LF newlines (so a Windows run matches CI Linux), then runs `npm run gen:api`. Generated client committed under `frontend/src/shared/api/**` (17 files: `sdk.gen.ts`, `types.gen.ts`, `@tanstack/react-query.gen.ts` with `getHealth`/`listAccounts` + `*Options`, `client/*`, `core/*`). Explicit `operation_id`s added to both routes (`getHealth`, `listAccounts`) for clean hook names. New **CI job `gen-api`** (`.github/workflows/ci.yml`): uv + Node 20, `npm ci`, `uv run python -m tools.gen_api`, `git diff --exit-code frontend/src/shared/api frontend/openapi.json` → red on stale client. The whole **`frontend/` tree excluded from the Python-side gates** (it has its own gate set per the FSD ADR): `tools/aislop_gate.py` (`_EXCLUDE` + `_EXCLUDED_PREFIXES` now cover `frontend/`), `.semgrepignore`, `.pre-commit-config.yaml` exclude, and `.gitignore` (`frontend/node_modules`, `frontend/dist`, `frontend/.vite`). Generation is **idempotent** (a second run yields zero git diff — the gate's core invariant, verified locally). ruff/ty/aislop clean; full suite green. `Blocked by #164` (merged). **Merged to `main` via PR #178.**

- **Split-stack pivot — S4 #166: React+TS+Vite FSD scaffold + gates + tracer** (branch `feat-166-react-fsd-scaffold`, 2026-06-28) — **Done (code), tracer built test-first.** The frontend walking skeleton. **Stack** (in `frontend/package.json`): React 19 + TypeScript strict + Vite 6; TanStack Router (code-based) + Query; `@sentry/react`; react-i18next; Tailwind v3 + `tailwindcss-animate` + the shadcn `cn` helper (clsx + tailwind-merge); self-hosted `@fontsource/inter` + `@fontsource/jetbrains-mono` + `flag-icons` (**zero CDN** — verified in the build output). **FSD layers** all present under `src/`: `app` (providers: QueryClientProvider, Sentry.ErrorBoundary, i18n side-effect init, RouterProvider), `routes` (root + index route → AccountsPage), `pages/accounts` (the tracer), `entities/account` (re-exports the generated `listAccountsOptions` as `accountsQueryOptions` — data only via shared/api), `widgets`/`features` (empty skeletons, `.gitkeep`), `shared` (`api` generated client, `lib/cn`, `i18n` ru+en fallback ru). **Design tokens** extracted from `web/` into `tailwind.config.ts` (dominant palette: `#0066ff` primary, `#0b0b0c` ink, `#f1efed` canvas, muted/line/success/danger/warning families). **Tracer** (`AccountsPage`): renders `/api/v1/accounts` via the generated TanStack Query hook (loading/empty/error/data), proving api → generated client → render. **Gates** wired into CI (new `frontend` job: `npm run gates` = Steiger boundaries + ESLint `--max-warnings 0` + Prettier `--check` + `tsc --noEmit` strict + Vitest, then `npm run build`): Vitest coverage floor **80%** (generated/shadcn/i18n/app/routes/barrels excluded; tested files at 100%). Dev `/api`→uvicorn proxy in `vite.config.ts`; prod `frontend/dist` served by FastAPI (from #164). **Test-env notes** (`# why`): split `vite.config.ts`/`vitest.config.ts` (dual-vite type clash); **happy-dom** (not jsdom) so the generated client's `new Request(url,{signal})` accepts TanStack Query's AbortSignal and resolves relative URLs; `vitest.setup.ts` stubs `fetch` + sets the client baseUrl (the client captures `globalThis.fetch` at import). Steiger `fsd/insignificant-slice` disabled (the ADR wants the skeleton from day one; single-consumer slices are expected pre-#167). `frontend/node_modules`+`dist`+`.vite` gitignored. The generated client was reformatted to the project `.prettierrc` (single-quote); gen-api drift gate re-verified idempotent. ruff/ty/pytest untouched (no Python change); aislop clean (frontend excluded, 150 files); mex 97/100. `Blocked by #164, #165` (both merged). **Merged to `main` via PR #179.**

- **Split-stack pivot — S5 #167: Accounts screen end-to-end (the pattern-setter)** (branch `feat-167-accounts-screen`, 2026-06-28) — **Done (code), test-first.** First real vertical feature; sets the `entity → widget → page → data` convention for #169–#172. **Backend** (`api/v1/accounts.py`, thin over existing `services.accounts` — business logic untouched): `POST /accounts/check`, `POST /accounts/profile`, `DELETE /accounts/{id}` (204), plus **multipart** `POST /accounts/import-tdata` (UploadFile + label) and `POST /accounts/photo` (account_id + UploadFile); ValueErrors → 400 envelope; explicit `operation_id`s. Added `python-multipart` dep (FastAPI's form backing; deptry DEP002-ignored — imported lazily by fastapi). **Frontend** (FSD): `entities/account` (`model/status` `accountHealth` mirroring the backend, `ui/StatusBadge` i18n badge, `api` query+mutation re-exports of the generated hooks), `widgets/accounts-table` (TanStack Table — status badge + `flag-icons` country flag, header sorting, row selection, row actions), `pages/accounts` (toolbar: search + status filter + tdata-import file button; cursor-stack pagination; wired check/delete/import mutations with query invalidation). All display strings + status labels in `shared/i18n` ru/en (locale-neutral API → FE labels). New deps `@tanstack/react-table` + `@testing-library/user-event`. **Tests**: 8 api endpoint tests (httpx ASGITransport; services mocked for Telegram-touching paths, real delete) → `api/v1/accounts.py` 100%; FE status/badge/table/page tests (loading/empty/error/data/pagination/check-action). Shared api-test fixtures moved to `tests/api/conftest.py`. `add-frontend-slice.md` cites this slice as the reference pattern. **731 tests green; ruff/ty/aislop/bandit/deptry clean; api/ 100%; FE gates (boundaries/eslint/prettier/tsc/vitest/build) green; gen-api drift idempotent; mex 97/100.** # ponytail: cursor-stack pagination + broad `invalidateQueries()` on mutation success (fine for one active query); tdata read into memory (stream if huge archives appear). `Blocked by #166` (merged). **Merged to `main` via PR #180** (CI caught + fixed a real env bug: steiger's web-tree-sitter WASM breaks on Node 20 → all CI Node jobs pinned to 24).

- **Split-stack pivot — S6 #168: auth — users + JWT cookie login + protected gate** (branch `feat-168-auth`, 2026-06-28) — **Done (code), test-first; OPERATOR-GATED (PR open, awaiting review before merge).** End-to-end auth, single-tenant, multi-user-ready. **Backend**: `core/auth.py` (argon2 hash/verify + JWT encode/decode — the only place tokens are minted; verify catches both `Argon2Error` and `InvalidHashError`); `users` table (SQLAlchemy `_users` + migration **#17**, `role` from day one); `core/repositories/users.py` (create/get-by-username/get-by-id/count → `UserRecord`); `services/auth/` (policy: `authenticate` with a **timing-equalizer dummy-hash** to block username enumeration, `resolve_user`, `issue_session_token`, `seed_admin_if_empty`; `_ratelimit` in-memory per-IP login limiter); `api/deps.py` `get_current_user` (cookie → `services.auth.resolve_user`, else 401; **re-issues the cookie each request = sliding TTL**); `api/v1/auth.py` `login`/`logout`/`me` (login sets the **HttpOnly+Secure+SameSite** cookie, 503 if `secret` unset, 429 when rate-limited); `api/v1/__init__` protects the accounts router with `Depends(get_current_user)` (auth + health public). Admin seeded at startup from `settings.auth.admin_username/password` when no users exist (no public signup). `core/migrations.py` split → `core/migration_steps.py` (bodies) for the aislop 400-line budget. New `settings.auth` knobs mirrored in `.env.example`. **Frontend** (FSD): `shared/auth` (re-exports `meQueryOptions`/`loginMutation`/`logoutMutation`), `pages/login` (form → mutation → navigate), protected **layout route** (`beforeLoad` ensures `/me` else `redirect('/login')`), global **401→/login** on the QueryClient (moved to `shared/lib/query-client.ts` so `routes` needn't import `app`). Cookie auto-sent (SPA same-origin with the API). **Tests**: core/services/api auth + FE login-page; feature tests use a `get_current_user` override in `tests/api/conftest.py`. aislop gate gained an alias-filter for `argon2`(argon2-cffi)/`jwt`(PyJWT). **756 backend tests green; FE gates green; ruff/ty/aislop/vulture/radon/bandit/deptry/semgrep/pip-audit clean; mex 97/100.** Security: stateless JWT (logout clears the cookie; the token isn't server-revoked until exp — per the no-rotation ADR, operator-accepted); **`AUTH__SECRET` ≥32 bytes hard-enforced** by an `AuthSettings` validator (operator-requested hardening; server-side revocation deliberately skipped as YAGNI). `Blocked by #166, #167` (merged). Operator-reviewed → approved with the secret-length validator. **Merged to `main` via PR #181** (CI caught + fixed: semgrep flagged the test-helper fixture passwords → `tests/` added to `.semgrepignore`, mirroring bandit).

- **Split-stack pivot — S7 #169: Warming screen end-to-end** (branch `feat-169-warming-screen`, 2026-06-28) — **Done (code), test-first.** **Backend** `api/v1/warming.py` (thin over existing `services.warming`): `GET /warming/board` (`WarmingBoardState` — whole board per poll), `POST /warming/start`+`/stop` (`UnknownAccountError`→404, `WarmingNotReadyError`→400), `GET`+`POST /warming/channels` + `POST /warming/channels/remove`, `GET`+`PUT /warming/settings`; protected via `Depends(get_current_user)`. **Frontend** (FSD): `entities/warming` (`WarmingStateBadge` + board query/mutation re-exports), `widgets/warming-board` (idle/warming columns of account cards + start/stop), `pages/warming` (polling board + channels add/remove + warming-settings toggles, via mutations + invalidation). **Navigation**: new `widgets/nav` — `AppNav` (Link to Accounts/Warming, `.active` styling) + `AppShell` (nav + `<Outlet/>`); the protected layout route renders `AppShell` (kept out of `router.tsx` to satisfy react-refresh/only-export-components). New `/warming` route. i18n ru/en for warming + nav. **Tests**: 6 api endpoint tests (mocked service; 404/400 paths) + FE state-badge/board/page tests. `widgets/nav` excluded from coverage (presentational chrome). **762 backend tests green; FE gates green; gen-api drift idempotent; ruff/ty/aislop clean; mex 97/100.** `Blocked by #167` (merged). **Merged to `main` via PR #182.**

- **Split-stack pivot — S8 #170: Neurocomment screen end-to-end** (branch `feat-170-neurocomment-screen`, 2026-06-28) — **Done (code), test-first.** **Backend** `api/v1/neurocomment.py` (thin over existing `services.neurocomment`): `GET`+`POST /neurocomment/campaigns`, `GET /campaigns/{id}/board` (`load_neurocomment_board`→404 when None), `POST /campaigns/{id}/channels` (link → `ChannelLinkOutcome`), `POST /campaigns/{id}/accounts` (assign, 204), `GET /runtime` + `POST /start`+`/stop` (listener); protected. 3 new request schemas (`LinkChannelRequest`/`AssignAccountRequest`/`StartNeurocommentRequest`). Challenge-audit controls (retry/skip/solver-toggle/counters) deferred — out of #170's core scope. **Frontend** (FSD): `entities/campaign` (`ChannelStatusBadge` covering the 7 `ChannelStatus` incl. `chat_restricted`/`bot_challenge`/`bot_challenge_backoff` + board/runtime query + create/link/assign/start/stop mutation re-exports), `widgets/neurocomment-board` (channel rows + status badges + account cards), `pages/neurocomment` (campaign select + create form, runtime status + listener select + start/stop, per-campaign link-channel + assign-account, polling board). New `/neurocomment` route + nav link. i18n ru/en. **Tests**: 7 api endpoint tests (mocked service; board-404) + FE badge/board/page tests. **769 backend tests green; FE gates (boundaries/eslint/prettier/tsc/vitest/build) green; gen-api drift idempotent; ruff/ty/aislop clean; mex 97/100.** `Blocked by #167` (merged). **Merged to `main` via PR #183.**

- **Split-stack pivot — S9 #171: Logs screen end-to-end (polling)** (branch `feat-171-logs-screen`, 2026-06-28) — **Done (code), test-first.** **Backend**: `core/repositories/logs._list_filtered_logs` + `list_filtered_logs` gained an additive `offset` param (default 0); new `services.logs.list_logs_page(log_filter, cursor) → Page[LogEntry]` (opaque-offset cursor, fetch `limit+1` to detect a next page; local `_decode_cursor` + `InvalidCursorError`); `api/v1/logs.py` `GET /logs` → `Page[LogEntry]` (query: status/account_id/cursor/limit; bad cursor→400); protected. **Frontend** (FSD): `entities/log` (`LogStatusBadge` success/warning/error + `logsQueryOptions` re-export), `pages/logs` (status + account filters, log table, cursor prev/next pagination, **polling** tail). New `/logs` route + nav link. i18n ru/en (event-code→label localization is the staged refinement — events render as codes for now). **Tests**: 4 api endpoint tests (real `log_event`-seeded rows; cursor pagination, status filter, bad-cursor 400) + FE badge/page tests (rows/empty/pagination/filters). **773 backend tests green; FE gates green; gen-api drift idempotent; ruff/ty/aislop clean; mex 91/100 (0 errors; the extra warnings are by-design commit-based STALE_FILE at the 50-commit threshold from this migration's commit volume + the known TOOL_CONFIG_DRIFT).** `Blocked by #167` (merged). **Merged to `main` via PR #184.**

- **Split-stack pivot — S10 #172: Settings screen (TanStack Form + Zod)** (branch `feat-172-settings-screen`, 2026-06-28) — **Done (code), test-first.** **Frontend-only** — reuses the existing `GET`/`PUT /warming/settings` endpoints (#169) as the canonical user-settings store (no backend change → zero API drift). New deps `@tanstack/react-form` + `zod`. `pages/settings` is a **TanStack Form + Zod** form over the warming settings (5 toggles + quiet-hours start/end with a 0–23 Zod range + Gemini model (required) + Gemini API key (blank = keep, `has_gemini_key` placeholder)); `onChange` Zod validation, localized error messages, `canSubmit`/`isDirty`/`isSubmitting`-gated submit with a Saved state. **Consolidation**: the convenience settings-toggle strip on `pages/warming` (#169) was removed — the Settings screen is settings' home now (no duplicate editing surface). New `/settings` route + nav link. i18n ru/en. **Tests**: FE — load+save (PUT fired) + validation (empty model → error). **773 backend tests green (unchanged); FE gates (boundaries/eslint/prettier/tsc/vitest/build) green; no API drift; aislop clean.** `Blocked by #167` (merged). **Merged to `main` via PR #185.**

- **Split-stack pivot — S11 #173: remove the verbatim `web/` SPA + its gate-exclusions** (branch `chore-173-remove-web-spa`, 2026-06-28) — **Done (code).** The React SPA reached parity across #167–#172, so the transitional design SPA and its tooling carve-outs are gone. Deleted **`web/index.html` + `web/support.js`**. `main.py`: dropped `_WEB_DIR` + the `web/` static-serve fallback (`_static_root` inlined into `_mount_frontend`, which now serves **only** `frontend/dist`); module docstring de-transitionalised. Removed the `web/` exclusions from **`tools/aislop_gate.py`** (`_EXCLUDE` + `_EXCLUDED_PREFIXES` → `frontend/` only), **`.semgrepignore`**, and the **`.pre-commit-config.yaml`** top-level `exclude` regex. `.mex` law refreshed: dropped the `web/` File Map line + de-transitionalised the token/aislop notes in `AGENTS.md`/`context/ci.md`/`context/frontend.md` (architecture.md's File Map already had no `web/` line). The only surviving `web/index.html`/`web/support.js` mention is the **historical changelog entry** above (the pivot that created `web/`) — GROW appends, it doesn't rewrite past history. **Backend `services`/`core`/`schemas` untouched** (this is pure removal). 773 backend tests green (unchanged — no `web/` test coverage existed; `main.py` is outside the coverage source); ruff/ty/aislop/vulture/radon/bandit/deptry/semgrep/pip-audit clean; FE gates green. `Blocked by #167, #168, #169, #170, #171, #172` (all merged). PR against `main`.

## Not Yet Built

- **Neurocomment feature** — design specced via grill (2026-06-22; see `context/decisions.md`). Separate **event-driven** `services/neurocomment/` package + `features/neurocomment/` page; campaign model (`campaign_id`); dedicated listener + warmed-pool commenters; pre-join + captcha at onboarding; new typed actions `CommentOnPost`/`ClickButton`/`GetLinkedDiscussionGroup`; image-captcha (Gemini vision) extends `core/gemini.py` later. Issues заведены на трекере: milestones Ф0–Ф3 (нейрокомментинг); **#113** (Ф0 спайк, `ready-for-human`, **гейт**) + **#114–#119** (Ф1 MVP: данные / Telegram-экшены / listener / онбординг / движок реакции / UI+канарейка). **Ф0 #113 — GO**: спайк доказал `comment_to` end-to-end (Marina-посторонний → реальный join → коммент); throwaway-прототип удалён (отработал); тест-канал `@neurocomment_spike_113` оставлен Ф1-фикстурой. **Ф1 MVP (#114–#119) — Done и в `main`** (PRs #122→#127; см. секцию Working). Капча в #117 сведена к detect-and-skip; решение капчи → спайк #120 (Ф2). **Осталось вне кода:** живая канарейка на 1–2 аккаунтах — шаг оператора (HITL), намеренно не запущена. Дальше — Ф2 (решение капчи #120, image-captcha через Gemini vision) и Ф3 (продвинутый прогрев/масштаб). **Ф2 «Защита от детекта» заспечена** (grill 2026-06-23, решения в `context/decisions.md` Ф2-update + `context/neurocomment.md` Planned-блок): **#131 (deletion-sweep → escalating channel back-off, новый gateway-экшен `CheckMessagesAlive`) и #132 (cross-account semantic-дедуп, token-set Jaccard) — оба Done и в `main`** (PR #135 / #134; см. секцию Working). Дальше по Ф2 — спайк #120 (решение капчи). (Имя `neurocomment` сменило плейсхолдер `services/comments/`.)
- Shared scheduler / `core/scheduler.py` — deliberately not used for warming; neurocomment uses an event listener, not cron. Add a real scheduler only if a future feature needs true cron semantics.

## Known Issues

- `aislop --version` can fail on Windows due to a space in the Python path. Use `uv run python -m aislop` if direct CLI invocation fails.

## Open Decisions

Authoritative list of architectural unknowns. Context files may carry `[TO BE DETERMINED]` markers; this section is the single index of all of them.

### Architecture / design

- **Account lifecycle enum beyond session health** — session health is stored on `accounts.status`; warming/runtime lifecycle lives separately in `warming_account_state.state`. A unified business lifecycle is still undecided. (`context/telegram.md`)
- **NiceGUI Logs page pagination** — limit + offset strategy on the SQLite `logs` query. (`context/logging.md`)
- **Project purpose / "why"** — deliberately deferred; not documented anywhere.

### Tooling / process

- **Mutation testing (`mutmut`)** — consider adding once critical modules stabilize. Start as nightly/workflow_dispatch only; do not gate PR merges until signal is proven.

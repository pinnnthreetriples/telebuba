---
last_updated: 2026-07-16
---

# MEX Sync
```bash
npx mex-agent check
npx mex-agent sync --dry-run
npx mex-agent doctor
```

Fix only reported drift. Keep live state in `ROUTER.md`, current invariants in `context/`, reusable steps in `patterns/`, and history/rationale in git plus `mex log`.
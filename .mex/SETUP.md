---
last_updated: 2026-07-16
---

# MEX Setup
The scaffold is populated. From the repository root use:

```bash
npx mex-agent check
npx mex-agent sync --dry-run
npx mex-agent doctor
```

Developer environment setup is in `context/setup.md`. Do not rerun `mex setup` unless rebuilding the scaffold intentionally.